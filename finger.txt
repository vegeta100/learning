第一二三次录入指纹，都放到一个子程序里，

删除的那些，要有个确定对话框

项目型和NFC卡型可以用指纹。
手机是带指纹的项目型

验证管理员指纹，在nfc的界面之前，
在一个布局里加载fragment，之后有需要再将这个布局给去掉
对，就是这样。或者是把这个fragment给隐藏掉

[200~NfcCallback process = new NfcCallback() {
        @Override
	        public void DoProcessIntent(Intent intent) {
		            processIntent(intent);
			            }
				        };

					大致猜想一下，那就是重写借口，只要有一个借口调用到，那么就按照它的方式，在跳转界面之前，先获取到spot的数据。可能需要一点延时什么的。

在need check里面，要改这个属性

needcheck为true，相对应的字段为0

验证指纹这个，以后的思路是，把显示信息和指纹验证都作为fragment，验证过后，就用显示信息的fragment替代它。

实际上，接口和数据库表都不多，我还是可以把它们重构的。

扫码巡检 改为人员管理
要知道这个指纹验证是否是代检
知道代检有个鬼用啊，

z8000，指纹和人员关联起来，需要z8000连上电脑，电脑里有人员，机器有指纹，这两个就可以关联了。把电脑的人员数据抓取到机器里边。就已经是关联上了。

指纹和人员是怎么关联的。人员给定一个编号，指纹也有编号，若编号相同，则关联上了？
写入指纹，就是为了下载指纹用的，下载后当然不可能传到数据库里面啊，它和一般的数据可不一样，需要将下载后的指纹写入到底层里。这样才可以验证指纹，
指纹下载，同时也要下载人员，人员至少需要指纹编号这个东西。指纹编号这个东西，不同的机器是可以重复的，这个倒不是问题。
或许我这边需要同时发起两个请求，

还有个问题，如果数据库已经有指纹了，只是下载人员，那我想问，这个要怎么关联，上传指纹、下载人员，通过指纹编号来关联的。
这还需要数据库查询语句，毕竟人员就是一般数据了。验证指纹得出编号后，再根据编号查询数据库。
应该还需要知道是哪台设备上传的才对，上传指纹可能也需要编号，读指纹是读出指纹数据，查询指纹是查出指纹编号。

后台怎么知道上传的指纹，是哪个对应哪个的？z8000连电脑，那个录入指纹的人在。可以比较好地确定要怎么关联
下载人员的时候，和指纹一起下载，先下载人员，再下载指纹，根据人员的指纹编号写指纹。`

先上传指纹，隔断时间再下载。或许可以通过通知来告诉你。
webservice地址和方法名都好定 ，关键是参数， 回调是我这里处理的。指纹数据、编号、类型
上传下载还是要在电脑旁边比较好，暂时先这样子。
上传指纹的时候是单独的，关联指纹的时候是不需要上传下载的，关联后指纹后再上传，要同时上传人员吗？
那也不能随便人员关联啊，需要有权限啊，这个权限是谁能有的呢？又得新建个表吧。
单纯在手机操作，工作量会很大的。

即便后面要做关联在手机端做，也是后面的事。不会太冲突的。
账号和z8000的没账号是有相当差别的。更换操作员，就是代检的。

除了指纹模块没关闭之外，还可能指纹模块打开失败的
我可以用另一个数据库的，用那个试着费
或者变慢也ok，一份数据村两个数据库``

下载当前用户的指纹，有几枚就下载几枚
分析需求，
上传指纹、删除管理员、下载人员。

下载指定指纹

Z6500，无管理员直接操作，有管理员验证管理员，
可以删除管理员，
下载人员，可以检测是否代检
不是纯指纹，而是和指纹相关的人员。
删除管理员是不是最好 的操作？

下载人员，就和更新人员一个样。未知人员，
上传指纹是为了备份，上传的指纹还要传那个登录账号

下载指纹前，先清除本地所有指纹。这样是挺合理的。

还有，那就是，一个操作员最多注册3枚指纹

在当前账户注册3枚指纹，不对，是可以注册任意枚，绑定对一个用户来说，最多绑定3枚。
下载人员，下载人员，人员所在公司的所有 人员。这台设备可以给公司的其他人用。

`
现在指纹登录，要怎么做呢？我想一下，
传指纹、传用户，但就是密码不会传，
不，我直接封装成一个类就成了吧。不用传指纹编号，登录那里多一个指纹验证的字段就成。就只是登录信息没了，其他数据库信息是还在的，而且，现在的设备也不多
到时候，一定要把登录放在前面的地方，就算放后面也不能也Gudb的数据库，得用我的才好。但如果这样的话，所有的接口都要改吧。一改动就这么地大。
不，我还可以这么弄，那就是，把这个需要保存的字段，还是继续保存给guarddB，这样的话，就暂时没问题了。这个项目的修改，慢慢来吧。这样子，下一次，去掉了guardDB，也是可以没问题的。
指纹登录或许可以这么设计，勾上指纹登录，查出来后，直接登录，然后主界面肯定也要显示名字的

勾上可指纹登录后，就可以验证了，当然需要给个提示，告诉用户指纹验证。勾上和取消都要吗？先简单做吧，后面再慢慢来
可以提示，查无匹配指纹，也可提示说当前指纹未绑定人员，不可登录

在服务器配置那里，要有一个测试按钮，测试webservice是否可用
测试远程接口是否是通的


有个bug，那就是熄屏后，录入指纹，再那个退出，就会白屏
这个已经改了，捕获了异常。第三次那里肯定也捕获了异常，只是，指纹上传的时候，应该怎么办呢？

显示多枚指纹，这需要数据库多一个新的字段吗?
人员多一个新的字段，在下载的时候处理会比较好。没错
而像保存指纹实体这样的代码 ，改成saveOrInsert也会好点，防御性编程
