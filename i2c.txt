e2prom
电可擦除可编程只读存储器
编程可以理解我置入数值

而flash存储器也是可擦除可编程的，不过擦除的范围和e2prom不同

i2c是一种数据存储方式，这样子，才能存放一些重要的数据之类的
我还可以用一下纯纸笔来些i2c，这样就足够应付面试的了。

搞懂上拉、下拉的含义，
上拉：接到vcc，上拉为高电平
下拉：接地，为低电平

那么，上拉、下拉的意义是什么呢？
上拉，将输入驱动到逻辑1？

输入：

最起码我要回答出i2c和spi的时序来。
必要的话，还要能用笔写出来才行。

上拉电阻，就是将一个不确定的电子信号驱动为高电平。而且，还是将输入的信号驱动为高电平。
其实，我当初学m4的时候，就没怎么去搞懂这一块吧。但，基本的概念是需要了解的。

多了一个上拉电阻，相当于并联， 那电阻是更小还是更大呢？我去。
如果是电阻更小的话，那就不算限流吧？

我发给桂勇的那个图，接地之后就变成〇了，所以，它知识将不确定的电子信号驱动为高电平，确定的电子信号，该是〇的话，那就还是〇，对吗？

糟糕的心情，才能读一读那个的。蒋新磊的小说，糟糕的心情，看了玛丽和马克斯才会哭泣。

中断，就不用while循环一直等待了，可以节省cpu，使cpu干更多的事

不用太深入，但是最起码要面面俱到，这样才过得了关。

flash和e2prom挺像的，只是一个用来存放单片机程序，一个用来存放某些关键字段，
flash 1M字节
e2prom
多大呢？e2prom可按字节擦除，这意味着它可以存多个字段，毕竟有多个字节吗
flash的话，是用来加载程序的

AT24c02
2k bit 256Byte
24c04 4k bit 512Byte
08 16什么的依此类推

波特率：115200，即1秒传播115200个信号，在单片机，一般1个信号是1bit，在这种情况下，波特率和比特率数值相等。
波特，可以理解为信号吧

定时器的计数方式，向上、向下，中间
不用怕的，单片机的有些东西，多加了也没事的。
先向上计数，向下计数，先向上再向下计数，那向上/下的基准线是一样的吗？

i2c没有spi那样的从设备选择线，主设备发送器件地址、再发送器件上的字节地址、再发送数据
这是写的，读的话，就麻烦一些
读是从设备发送给主设备，所以地址呢，就是你刚好写到哪，就从那里开始读
器件地址 写信号 停止、起始 器件地址 读了

spi的话，有模式0和模式3
串行输出线 串行输入线
从设备选择线
时钟信号线

片选信号线拉低，是一个从设备就会配一个片选信号线吧。
片选信号线拉低，然后才有意义
然后我讲其中一个模式就行了，模式0吧
模式0或模式3
先片选信号线拉低，在上升沿的时候采集数据，下降沿的时候将数据传输出去。



蓝牙打印机
烧录字库的方式，
步进电机是用PWM，脉冲宽度调制驱动步进电机的

协议只是存储和读取数据的一种方式，AT24c02 w25q64，前者可以按字节擦除，后者就是flash
所以才会有字典说两个差不多


