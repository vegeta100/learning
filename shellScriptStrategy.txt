1.4 shell进行数学运算

let，其参数是算术运算表达式，变量就是数字
而表达式中$a是a的值，并不是变量a。把$a放等式左边就是错的。

同样地，$[]将字符串变成了算术运算表达式。
也就是说，[]才能装表达式，和if[]是一个道理。

$() ``
这两个都是包含命令的，
$[] $(())
这两个是包含算术运算表达式的。
从上到下，是等级高了，所以，从命令变成数学表达式了。

以上是整数相关的运算，所以，shell脚本里只能用整数运算？
脚本不是应用程序，整数就够了。需要小数的时候，再用需要的工具。
